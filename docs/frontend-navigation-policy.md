# 前端跳转入口策略

本前端不提供跳转到外部站点的入口。顶部标题、帮助弹窗、支持弹窗和设置页均应保持站内交互，不再展示 GitHub、赞助、反馈等外部跳转按钮。

设置页不再提供“关于”页签。Markdown 内容中的链接仅按文本展示，不触发页面跳转。

API 服务商下拉默认只展示 OpenAI 兼容接口和用户创建的自定义服务商，不再默认展示 fal.ai。历史 fal 配置和任务兼容逻辑仍保留，避免旧数据恢复时出错。
